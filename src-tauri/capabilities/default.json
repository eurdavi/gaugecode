{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the tray popup and the settings window. The notch window is added in M2.",
  "windows": ["popup", "settings"],
  "permissions": [
    "core:default",
    "opener:default",
    "positioner:default"
  ]
}
