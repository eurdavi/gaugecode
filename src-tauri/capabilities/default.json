{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline for every window: IPC and positioning. Overlay windows get nothing else.",
  "windows": ["notch", "bar", "popup", "settings"],
  "permissions": [
    "core:default",
    "positioner:default"
  ]
}
